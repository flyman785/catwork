import { HostlistClickDirective } from './hostlist-click.directive';

describe('HostlistClickDirective', () => {
  it('should create an instance', () => {
    const directive = new HostlistClickDirective();
    expect(directive).toBeTruthy();
  });
});
